Todo:
    Make sure that the states are being saved properly with unit tests
    Look through the code and assure there are no methods that are called twice
    Include the last frame into the simulation

